#include <cstdio>
#include <cstring>

using namespace std;
int l[10000];

int main()
{
  char x[10000] = {0}, y[10000] = {0};
  int n = 0, m = 0;
  scanf("%s%s", x, y);
  while (x[++m] != 0) {};
  while (y[++n] != 0) {};
}